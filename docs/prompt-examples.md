# Prompt Examples

Use these examples as starting points. Keep private data out of prompts unless your app has the right user consent and data handling controls.

## 1. Generate a short narration draft for a product walkthrough.

```text
Generate a short narration draft for a product walkthrough.
```

## 2. Create dialogue audio for a creator-tool prototype.

```text
Create dialogue audio for a creator-tool prototype.
```

## 3. Request timestamps so generated speech can line up with captions or video cuts.

```text
Request timestamps so generated speech can line up with captions or video cuts.
```

## Practical Notes

- Start with a small, repeatable test before putting the model behind a user-facing workflow.
- Save the model name and request settings with each result.
- Keep generated output reviewable before it is published or sent to customers.
